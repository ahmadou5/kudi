[providers]
providers = ["node"]

[phases.setup]
nixPkgs = ["nodejs_20", "nodePackages.pnpm"]

[phases.install]
cmds = ["npm install -g pnpm@8.15.3", "pnpm install"]

[phases.build]
cmds = ["pnpm build"]

[start]
cmd = "pnpm start"
