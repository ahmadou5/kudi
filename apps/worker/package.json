{
  "name": "@kudi/worker",
  "version": "0.1.0",
  "private": true,
  "engines": {
    "node": "20"
  },
  "scripts": {
    "build": "tsup src/index.ts --format cjs,esm",
    "dev": "tsx watch src/index.ts",
    "build:runtime": "pnpm --workspace-concurrency=1 --filter @kudi/worker^... run build:runtime && pnpm run build",
    "start": "pnpm --filter @kudi/database db:sync && pnpm --filter @kudi/database db:generate && pnpm run build:runtime && node dist/index.js",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "@kudi/chains": "workspace:*",
    "@kudi/database": "workspace:*",
    "@kudi/payment-providers": "workspace:*",
    "@kudi/types": "workspace:*",
    "dotenv": "^16.4.5",
    "@kudi/config": "workspace:*"
  },
  "devDependencies": {
    "tsx": "^4.7.1",
    "tsup": "^8.0.2",
    "typescript": "^5.3.3"
  }
}
