[build]
builder = "NIXPACKS"
buildCommand = "pnpm --filter @kudi/worker build"
watchPatterns = ["/apps/worker/**"]
buildEnvironment = "V3"

[deploy]
runtime = "V2"
numReplicas = 1
startCommand = "pnpm --filter @kudi/worker start"
sleepApplication = false
useLegacyStacker = false
ipv6EgressEnabled = false
multiRegionConfig = { sfo = { numReplicas = 1 } }
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
