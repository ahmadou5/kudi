[build]
builder = "NIXPACKS"
buildCommand = "pnpm --filter @kudi/admin build"
watchPatterns = ["/apps/admin/**"]
buildEnvironment = "V3"

[deploy]
runtime = "V2"
numReplicas = 1
startCommand = "pnpm --filter @kudi/admin start"
sleepApplication = false
useLegacyStacker = false
ipv6EgressEnabled = false
multiRegionConfig = { sfo = { numReplicas = 1 } }
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
